{
  "name": "me2u-example-browser",
  "version": "0.1.0",
  "description": "Testing `from-me-to-you` in the browser",
  "private": true,
  "scripts": {
    "build": "browserify index.js | exorcist bundle.js.map > bundle.js",
    "dev": "watchify index.js -o 'exorcist bundle.js.map > bundle.js' -d & serve",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "dependencies": {
    "from-me-to-you": "file:../../"
  },
  "devDependencies": {
    "browserify": "^16.5.0",
    "core-js": "^3.3.6",
    "exorcist": "^1.0.1",
    "regenerator-runtime": "^0.13.3",
    "serve": "^11.2.0",
    "watchify": "^3.11.1"
  }
}
